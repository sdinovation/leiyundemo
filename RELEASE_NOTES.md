# Release Notes — 数分精灵 v2.1

**日期**：2026-08-17
**范围**：纯前端版本，单文件部署（`index.html`）
**基线**：[BASELINE.md](tests/golden/BASELINE.md) v2.1 — 199/199 = **100% 通过**

---

## 🎯 本版本一句话总结

> 分析正确率达到黄金集全量 100%，LLM 调用统一加超时降级，不再卡死。

---

## ✅ 核心修复（直接关系"分析正确率"）

| 编号 | 问题 | 修复 | 影响测试 |
|---|---|---|---|
| **T112** | 大数据量（10k 行）下商品名匹配漏掉稀疏值 | `parseLikeFilter` 样本量 50 → 200 行 | T112（商品 LIKE） |
| **T175 / T194** | 闰年单日 `2024-02-29` 解析失败 | 新增 `exactDayMatch` 前置分支 + `Date.UTC()` 避免漂移 | T175、T194 |
| **T120 / T121** | "按月统计" 被错认为"按币种"分组 | `parseDimension.timeKeywords` 增补 `按月/分月/各月/每月/月度/按季度/分季度` | T120、T121 |
| **T197** | "销售额总和超过5000" 被错走 WHERE | 阈值分支识别显式聚合词（总和/累计/汇总/总计/总额/合计）→ 优先 HAVING | T197 |

**结果**：黄金集 4 个指标全部 100%

| 维度 | 修复前 | 修复后 |
|---|---|---|
| SQL 准确性 | 192/199 (96.5%) | **199/199 (100%)** |
| 数据正确性 | 195/199 (98.0%) | **199/199 (100%)** |
| 解读存在率 | 199/199 (100%) | **199/199 (100%)** |
| 反幻觉通过率 | 199/199 (100%) | **199/199 (100%)** |

---

## ✅ 鲁棒性修复（直接关系"上线后能否用"）

| 编号 | 改动 | 关键点 |
|---|---|---|
| **D1** | `callLLMviaProxy` 加 25 秒 AbortController 超时 | 代理卡死时不再永久转圈 |
| **D1.3** | 解读阶段 LLM 失败 → `showToast('AI 解读超时/失败，已用本地规则', 'warn')` | 用户能看到降级 |
| **D1.4** | 综合 AI 增强失败 → `pushErrorLog + showToast` 双出口 | 错误可追踪、体验可感知 |
| **D1** | `fetchLLMWithTimeout` helper 已统一封装（15s / 20s / 25s / 30s 四档） | 所有 LLM fetch 都有超时兜底 |

---

## 🧪 黄金集（Golden Set）扩展

新增 4 个数据集（共 118 条新题）：

| 数据集 | 行数 | 测试题 | 验证重点 |
|---|---|---|---|
| `ecommerce_10k.csv` | 10,000 | 37 | 大数据量 + LIKE 稀疏匹配 |
| `finance.csv` | 24 | 30 | 财务多指标 + 月份/币种分组 |
| `dirty_data.csv` | 13 | 25 | 缺失值 + 脏数据容错 |
| `leap_year.csv` | 16 | 25 | 闰年单日 + 跨年时间 |

总计：**199 题 / 7 数据集 / 20 类别**

---

## 🛠️ 已知限制（设计层面保留）

| 场景 | 行为 | 说明 |
|---|---|---|
| 字段未被任何问句关键词提及 | 默认选第一个文本维度（如"币种"） | 上游可加问句提示词避免 |
| 用户 API Key 限流（429） | 静默重试 1 次后降级本地 | 已有 console 警告，无 toast |
| 30 万行以上超大 CSV | 前端解析耗时 8-12s | 超过该规模建议后端处理 |

---

## 📦 部署说明

- 单文件 `index.html`（约 1 MB，含 Tailwind/ECharts/PapaParse/lucide）
- 浏览器打开即可使用
- 可选：配置 LLM API Key（设置 → API Key 配置），否则走飞书代理（需代理可用）
- LLM 不可用时：分析仍能给出，本地规则保证数据正确，仅解读文字从"AI 深度解读"降级为"本地规则"

---

## 🔗 验证

```bash
node tests/golden/golden-runner.js
# 期望输出：总计: 199/199 通过 (100.0%)
```

依赖：Node.js ≥ 18、jsdom（`npm install jsdom`）